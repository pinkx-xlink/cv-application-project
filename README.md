# cv-application-project

Project: CV Application for The Odin Project (React course)

6/14/26 Overhaul
To-do:

- Split the input fields into categories (General info, Education, Experience) that serve as the headers for animated dropdown menus with actual input fields (General -> name, phone number, email)
- Render the CV on the right side of the page
- Make the CV downloadable
- Set default values so that there is an example CV when user opens the app
- Have a color picker for header bgColor
